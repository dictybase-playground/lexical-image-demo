{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint-config-dictybase": "*",
    "prettier": "^2.7.1",
    "turbo": "^1.7.4"
  }
}
